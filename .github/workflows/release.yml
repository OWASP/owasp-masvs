on:
  push:
    # Sequence of patterns matched against refs/GITHUB_REFs
    tags:
    - 'v*' # Push events to matching v*, i.e. v1.0, v20.15.10

name: Upload Release Asset
jobs:
  build:
    name: Upload Release Asset
    if: github.actor == 'commjoen' || github.actor == 'cpholguera' || github.actor == 'sushi2k'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 1
      - name: Generate python scripts (csv)
        run: docker run -v ${PWD}:/documents masvs-generator 'cd /documents/tools && python3 export.py --format csv --lang en > OWASP_MASVS-${GITHUB_REF}.csv'
      - name: Generate python scripts (json)
        run: docker run -v ${PWD}:/documents masvs-generator 'cd /documents/tools && python3 export.py --format json --lang en > OWASP_MASVS-${GITHUB_REF}.json'
      - name: Generate python scripts (xml)
        run: docker run -v ${PWD}:/documents masvs-generator 'cd /documents/tools && python3 export.py --format xml --lang en > OWASP_MASVS-${GITHUB_REF}.xml'
      - name: Generate English docs
        run: docker run --rm -u `id -u`:`id -g` -v ${PWD}:/pandoc masvs-generator '/pandoc_makedocs.sh Document $GITHUB_REF'
      - name: Generate Spanish docs
        run: docker run --rm -u `id -u`:`id -g` -v ${PWD}:/pandoc masvs-generator '/pandoc_makedocs.sh Document-es $GITHUB_REF'
      - name: Generate German docs
        run: docker run --rm -u `id -u`:`id -g` -v ${PWD}:/pandoc masvs-generator '/pandoc_makedocs.sh Document-de $GITHUB_REF'
      - name: Generate French docs
        run: docker run --rm -u `id -u`:`id -g` -v ${PWD}:/pandoc masvs-generator '/pandoc_makedocs.sh Document-fr $GITHUB_REF'
      - name: Generate Russian docs
        run: docker run --rm -u `id -u`:`id -g` -v ${PWD}:/pandoc masvs-generator '/pandoc_makedocs.sh Document-ru $GITHUB_REF'
      - name: Generate Japanese docs
        run: docker run --rm -u `id -u`:`id -g` -v ${PWD}:/pandoc masvs-generator '/pandoc_makedocs.sh Document-ja $GITHUB_REF'
      - name: Generate Traditional Chinese docs
        run: docker run --rm -u `id -u`:`id -g` -v ${PWD}:/pandoc masvs-generator '/pandoc_makedocs.sh Document-zhtw $GITHUB_REF'
      - name: Generate Simplified Chinese docs
        run: docker run --rm -u `id -u`:`id -g` -v ${PWD}:/pandoc masvs-generator '/pandoc_makedocs.sh Document-zhcn $GITHUB_REF'
      - name: Generate Korean docs
        run: docker run --rm -u `id -u`:`id -g` -v ${PWD}:/pandoc masvs-generator '/pandoc_makedocs.sh Document-ko $GITHUB_REF'
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          body_path: ${{ github.workflow }}-tools/RECENT_CHANGES.TXT
          files: |
            OWASP_MASVS-${GITHUB_REF}-de.epub
            OWASP_MASVS-${GITHUB_REF}-de.mobi
            OWASP_MASVS-${GITHUB_REF}-de.pdf
            OWASP_MASVS-${GITHUB_REF}-de_WIP_.docx
            OWASP_MASVS-${GITHUB_REF}-en.epub
            OWASP_MASVS-${GITHUB_REF}-en.mobi
            OWASP_MASVS-${GITHUB_REF}-en.pdf
            OWASP_MASVS-${GITHUB_REF}-en_WIP_.docx
            OWASP_MASVS-${GITHUB_REF}-es.epub
            OWASP_MASVS-${GITHUB_REF}-es.mobi
            OWASP_MASVS-${GITHUB_REF}-es.pdf
            OWASP_MASVS-${GITHUB_REF}-es_WIP_.docx
            OWASP_MASVS-${GITHUB_REF}-fr.epub
            OWASP_MASVS-${GITHUB_REF}-fr.mobi
            OWASP_MASVS-${GITHUB_REF}-fr.pdf
            OWASP_MASVS-${GITHUB_REF}-fr_WIP_.docx
            OWASP_MASVS-${GITHUB_REF}-ja.epub
            OWASP_MASVS-${GITHUB_REF}-ja.mobi
            OWASP_MASVS-${GITHUB_REF}-ja.pdf
            OWASP_MASVS-${GITHUB_REF}-ja_WIP_.docx
            OWASP_MASVS-${GITHUB_REF}-ko.epub
            OWASP_MASVS-${GITHUB_REF}-ko.mobi
            OWASP_MASVS-${GITHUB_REF}-ko.pdf
            OWASP_MASVS-${GITHUB_REF}-ko_WIP_.docx
            OWASP_MASVS-${GITHUB_REF}-ru.epub
            OWASP_MASVS-${GITHUB_REF}-ru.mobi
            OWASP_MASVS-${GITHUB_REF}-ru.pdf
            OWASP_MASVS-${GITHUB_REF}-ru_WIP_.docx
            OWASP_MASVS-${GITHUB_REF}-zhcn.epub
            OWASP_MASVS-${GITHUB_REF}-zhcn.mobi
            OWASP_MASVS-${GITHUB_REF}-zhcn.pdf
            OWASP_MASVS-${GITHUB_REF}-zhcn_WIP_.docx
            OWASP_MASVS-${GITHUB_REF}-zhtw.epub
            OWASP_MASVS-${GITHUB_REF}-zhtw.mobi
            OWASP_MASVS-${GITHUB_REF}-zhtw.pdf
            OWASP_MASVS-${GITHUB_REF}-zhtw_WIP_.docx
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      # "OFFICIAL" github actions only support one file ot upload. therefore will use unoficial
      # - name: Create Release
      #   id: create_release
      #   uses: actions/create-release@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     GITHUB_REF_name: ${{ github.ref }}
      #     release_name: Release ${{ github.ref }}
      #     draft: false
      #     prerelease: false
      # - name: Upload Release Asset
      #   id: upload-release-asset 
      #   uses: actions/upload-release-asset@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps 
      #     asset_path: ./my-artifact.zip
      #     asset_name: my-artifact.zip
      #     asset_content_type: application/zip
      